export type Role='Administrator'|'ED Physician'|'Laboratory Supervisor'|'Operations Manager';
export type Status='Unassigned'|'Assigned'|'Opened'|'Acknowledged'|'Deferred'|'Escalated'|'Closed';
export interface Physician{id:string;displayName:string;role:Role;specialty:string;authorisedTestGroups:string[];shift:'Day'|'Evening'|'Night';availability:'Available'|'Unavailable'|'Overloaded';online:boolean;activeAssignments:number;maximumConcurrentAssignments:number;medianAcknowledgementMinutes:number;lastAssignedAt:string;backupEligible:boolean;excluded:boolean;}
export interface LabResult{id:string;encounterId:string;patientAlias:string;ageBand:string;sex:string;triageCategory:number;testGroup:string;resultLabel:string;criticalFlag:boolean;orderedAt:string;collectedAt:string;releasedAt:string;assignedAt:string|null;openedAt:string|null;acknowledgedAt:string|null;closedAt:string|null;status:Status;priority:'Routine'|'Urgent'|'Critical';assignedPhysicianId:string|null;backupPhysicianId:string|null;shift:'Day'|'Evening'|'Night';waitingMinutes:number;delayRiskScore:number;delayRiskBand:'Low'|'Moderate'|'High';assignmentExplanation:string;escalationLevel:number;sourceSystem:string;interfaceLatencyFlag:boolean;}
export interface AuditEvent{id:string;timestamp:string;actor:string;role:Role;action:string;reason:string;previousOwner:string;newOwner:string;sourceSystem:string;}
export interface Alert{id:string;resultId:string;status:'Active'|'Acknowledged'|'Escalated'|'Resolved'|'False positive'|'Suppressed';severity:'Info'|'Warning'|'Critical';owner:string;message:string;createdAt:string;}
