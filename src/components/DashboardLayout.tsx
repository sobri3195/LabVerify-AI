import {NavLink,Outlet,useNavigate} from 'react-router-dom';import {Activity,AlertTriangle,BarChart3,ClipboardList,FileText,FlaskConical,LayoutDashboard,LogOut,Menu,Microscope,Settings,ShieldCheck,SunMoon,Users,X} from 'lucide-react';import {useState} from 'react';import {useApp} from '../AppState';
const links=[['/app', 'Overview',LayoutDashboard,true],['/app/results','Result queue',FlaskConical],['/app/alerts','Alert centre',AlertTriangle],['/app/physicians','Physicians',Users],['/app/cases','Cases',ClipboardList],['/app/analytics','Analytics',BarChart3],['/app/audit','Audit trail',ShieldCheck],['/app/reports','Reports',FileText],['/app/settings','Settings',Settings]] as const;
export function DashboardLayout(){const [open,setOpen]=useState(false);const {role,logout,theme,setTheme}=useApp();const nav=useNavigate();const side=<><div className="flex items-center gap-3 px-3 py-4"><span className="rounded-xl bg-cyan-500 p-2 text-navy-950"><Microscope/></span><div><b>LabVerify-AI</b><p className="text-[10px] text-slate-500">Every result has a next owner.</p></div></div><nav className="mt-4 space-y-1">{links.map(([to,label,Icon,end])=><NavLink end={end} onClick={()=>setOpen(false)} className={({isActive})=>`navlink ${isActive?'active':''}`} key={to} to={to}><Icon size={18}/>{label}</NavLink>)}</nav><div className="mt-auto panel p-3 text-xs"><span className="badge mb-2 border-cyan-500/30 text-cyan-500">Prototype</span><p className="font-semibold">{role}</p><p className="text-slate-500">Synthetic workflow view</p></div></>;
return <div className="min-h-screen"><aside className="fixed inset-y-0 left-0 z-50 hidden w-64 flex-col border-r bg-white p-3 dark:border-white/10 dark:bg-navy-950 lg:flex">{side}</aside>{open&&<div className="fixed inset-0 z-50 bg-black/60 lg:hidden" onClick={()=>setOpen(false)}><aside className="flex h-full w-72 flex-col bg-white p-3 dark:bg-navy-950" onClick={e=>e.stopPropagation()}><button className="ml-auto p-2" onClick={()=>setOpen(false)}><X/></button>{side}</aside></div>}<div className="lg:pl-64"><header className="sticky top-0 z-40 flex h-16 items-center justify-between border-b bg-white/85 px-4 backdrop-blur dark:border-white/10 dark:bg-navy-950/85 md:px-8"><button className="lg:hidden" onClick={()=>setOpen(true)} aria-label="Open navigation"><Menu/></button><div className="hidden items-center gap-2 text-xs md:flex"><Activity size={15} className="text-emerald-500"/><b>DEMO LIVE</b><span className="text-slate-500">· events are simulated</span></div><div className="flex items-center gap-2"><button className="btn-secondary px-3" onClick={()=>setTheme(theme==='dark'?'light':'dark')} aria-label="Toggle theme"><SunMoon size={17}/></button><button className="btn-secondary px-3" onClick={()=>{logout();nav('/')}}><LogOut size={17}/><span className="hidden sm:inline">Exit</span></button></div></header><div className="p-4 md:p-8"><Outlet/></div></div></div>}
