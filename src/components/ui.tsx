import {motion} from 'motion/react'; import {Info} from 'lucide-react';
export const SimBadge=()=> <span className="badge border-cyan-500/30 bg-cyan-500/10 text-cyan-600 dark:text-cyan-300">Simulated</span>;
export function Page({title,description,children,action}:{title:string;description:string;children:React.ReactNode;action?:React.ReactNode}){return <motion.main initial={{opacity:0,y:8}} animate={{opacity:1,y:0}} className="space-y-6"><header className="flex flex-wrap items-start justify-between gap-4"><div><div className="mb-2 flex items-center gap-2"><SimBadge/><span className="text-xs text-slate-500">Synthetic data only</span></div><h1 className="text-2xl font-bold tracking-tight md:text-3xl">{title}</h1><p className="mt-2 max-w-3xl text-sm text-slate-500">{description}</p></div>{action}</header>{children}</motion.main>}
export function Safety(){return <div className="panel flex gap-3 border-amber-500/30 bg-amber-500/5 p-4 text-sm"><Info className="shrink-0 text-amber-500" size={20}/><p><strong>Safety boundary:</strong> LabVerify-AI coordinates responsibility. It does not diagnose disease, interpret laboratory values, verify results, prescribe treatment, or determine discharge.</p></div>}
export const Status=({value}:{value:string})=><span className={`badge ${value==='Critical'||value==='Active'||value==='Escalated'?'border-red-500/30 bg-red-500/10 text-red-500':value==='Acknowledged'||value==='Closed'||value==='Resolved'?'border-emerald-500/30 bg-emerald-500/10 text-emerald-500':'border-amber-500/30 bg-amber-500/10 text-amber-500'}`}>{value}</span>;
