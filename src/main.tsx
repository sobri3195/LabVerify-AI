import React from 'react';import ReactDOM from 'react-dom/client';import {BrowserRouter,Navigate,Route,Routes,useLocation} from 'react-router-dom';import {QueryClient,QueryClientProvider} from '@tanstack/react-query';import {Toaster} from 'sonner';import {AppProvider,useApp} from './AppState';import {PublicLayout} from './components/PublicLayout';import {DashboardLayout} from './components/DashboardLayout';import {CaseStudy,ContentPage,Landing,Login} from './pages/PublicPages';import {Alerts,Analytics,Audit,Cases,Overview,Physicians,Reports,ResultDetail,Results,Settings} from './pages/DashboardPages';import './index.css';
const query=new QueryClient({defaultOptions:{queries:{staleTime:30000}}});
function Protected(){const {role}=useApp();const location=useLocation();return role?<DashboardLayout/>:<Navigate replace to="/login" state={{from:location}}/>}
function App(){return <Routes><Route element={<PublicLayout/>}><Route index element={<Landing/>}/><Route path="about" element={<ContentPage kind="about"/>}/><Route path="case-study" element={<CaseStudy/>}/><Route path="methodology" element={<ContentPage kind="methodology"/>}/><Route path="privacy" element={<ContentPage kind="privacy"/>}/><Route path="login" element={<Login/>}/></Route><Route path="app" element={<Protected/>}><Route index element={<Overview/>}/><Route path="results" element={<Results/>}/><Route path="results/:id" element={<ResultDetail/>}/><Route path="alerts" element={<Alerts/>}/><Route path="physicians" element={<Physicians/>}/><Route path="cases" element={<Cases/>}/><Route path="analytics" element={<Analytics/>}/><Route path="audit" element={<Audit/>}/><Route path="reports" element={<Reports/>}/><Route path="settings" element={<Settings/>}/></Route><Route path="*" element={<Navigate to="/" replace/>}/></Routes>}
ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><QueryClientProvider client={query}><AppProvider><BrowserRouter><App/></BrowserRouter><Toaster richColors position="top-right"/></AppProvider></QueryClientProvider></React.StrictMode>);
